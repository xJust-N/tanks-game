## инфо
1) архитектура, instanceof, pattern matching
	* да это нормально
2) collision handler с сеткой
	* сказал лучше массив но так тоже допустимо
	* надо выбрать подходящий размер сетки для ускорения обработки коллизий
3) управление с помощью blocking queue, флаг isMoving, update(int delta)
	* в целом норм, надо решить передавать дельту или координаты при перемещении, и так и так можно
4) протокол: клиентская карта-слушатель, передача всей карты в начале, передача сущности при обноновлении, передача координат при перемещении:
	* в начале передается карта(или синхронизация - передача карты через какой то тайминг)
	* передача сущности, координат - нормально
	* можно передавать или дельту или координаты(вроде это проще)
5) GameEvent: одно с типом ивента или несколько интерфейсов: 
	* если параметры такие же и типов немного то можно так
6) В начале: Получать от пользователя хост, порт? 
	* да

## TODO

1) Протокол
   * как передавать карту и обновления
   * сериализация / десериализация

2) В интенсивной игре часто используют udp тк он быстрее
если tcp не справится, переход на udp или оптимизация размеров пакета

3) Улучшить иерархию пакетов и модулей - общее в common, все что не общее убрать
интерфейсы

4) Несколько потоков на клиенте - нормально
на сервере лучше nio

## Другое:
* миграция в gitverse 
* окно запуска, присоединения